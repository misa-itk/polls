version: 2
jobs:
  build:
    working_directory: ~/polls
    docker:
      - image: haskell:8.2.1
    steps:
      - checkout
      - run:
          command: |
            pip install docker-compose
            docker-compose build